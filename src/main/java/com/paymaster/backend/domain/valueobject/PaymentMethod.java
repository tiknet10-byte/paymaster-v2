package com.paymaster.backend.domain.valueobject;

/**
 * **روش پرداخت** (Payment Method).
 * روش‌های مجاز برای ثبت پرداخت اقساط را تعریف می‌کند.
 */
public enum PaymentMethod {
    /**
     * پرداخت به صورت وجه نقد.
     */
    CASH("نقدی", "💵"),

    /**
     * پرداخت با استفاده از کارت‌های بانکی (به صورت مستقیم یا آنلاین).
     */
    CARD("کارت بانکی", "💳"),

    /**
     * انتقال وجه از طریق ساتنا، پایا یا سایر روش‌های بانکی.
     */
    TRANSFER("انتقال وجه", "🏦"),

    /**
     * پرداخت از طریق چک.
     */
    CHEQUE("چک", "📄"),

    /**
     * پرداخت از طریق دستگاه کارت‌خوان (POS).
     */
    POS("دستگاه POS", "🖥️");

    private final String persianName;
    private final String icon; // استفاده از آیکون‌های ایموجی یا نمادها

    /**
     * سازنده برای اختصاص نام فارسی و آیکون/نماد.
     * @param persianName نام فارسی روش پرداخت.
     * @param icon نماد یا آیکون مرتبط.
     */
    PaymentMethod(String persianName, String icon) {
        this.persianName = persianName;
        this.icon = icon;
    }

    /**
     * دریافت نام فارسی روش پرداخت.
     * @return نام فارسی.
     */
    public String getPersianName() {
        return persianName;
    }

    /**
     * دریافت نماد یا آیکون مرتبط با روش پرداخت.
     * @return آیکون.
     */
    public String getIcon() {
        return icon;
    }
}